.. _raster_edits:

Raster edits
------------

The rasters that are used in generating the :ref:`subgrid_tables` can be edited during the simulation. Within a specified area, all pixels can be given the same value, or their value can be increased or decreased by a specific amount.